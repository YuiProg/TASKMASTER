name: CI/CD

on:
  push:
    branches: [ "main", "staging" ]
  pull_request:
    branches: [ "main", "staging" ]

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: core
            path: taskmaster-backend/task-master-core
          - name: comment
            path: taskmaster-backend/task-master-comment
          - name: reports
            path: taskmaster-backend/task-master-reports
          - name: gateway
            path: taskmaster-backend/task-master-gateway
          - name: scheduler
            path: taskmaster-backend/task-master-scheduler
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
          cache-dependency-path: ${{ matrix.path }}/pom.xml

      - name: Build and run tests
        working-directory: ${{ matrix.path }}
        run: |
          if [ -f "./mvnw" ]; then
            chmod +x mvnw
            ./mvnw -B clean verify -DskipTests
          else
            mvn -B clean verify -DskipTests
          fi

      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '${{ matrix.path }}'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports-${{ matrix.name }}
          path: ${{ matrix.path }}/target/surefire-reports/*.xml
          if-no-files-found: ignore

  deploy:
    name: Trigger Render Deployments
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Core Service
        run: |
          if [ -n "${{ secrets.RENDER_DEPLOY_HOOK_CORE }}" ]; then
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_CORE }}"
          else
            echo "RENDER_DEPLOY_HOOK_CORE secret is missing!"
            exit 1
          fi

      - name: Deploy Comment Service
        run: |
          if [ -n "${{ secrets.RENDER_DEPLOY_HOOK_COMMENT }}" ]; then
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_COMMENT }}"
          else
            echo "RENDER_DEPLOY_HOOK_COMMENT secret is missing!"
            exit 1
          fi

      - name: Deploy Reports Service
        run: |
          if [ -n "${{ secrets.RENDER_DEPLOY_HOOK_REPORTS }}" ]; then
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_REPORTS }}"
          else
            echo "RENDER_DEPLOY_HOOK_REPORTS secret is missing!"
            exit 1
          fi

      - name: Deploy Gateway Service
        run: |
          if [ -n "${{ secrets.RENDER_DEPLOY_HOOK_GATEWAY }}" ]; then
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_GATEWAY }}"
          else
            echo "RENDER_DEPLOY_HOOK_GATEWAY secret is missing!"
            exit 1
          fi

      - name: Deploy Scheduler Service
        run: |
          if [ -n "${{ secrets.RENDER_DEPLOY_HOOK_SCHEDULER }}" ]; then
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_SCHEDULER }}"
          else
            echo "RENDER_DEPLOY_HOOK_SCHEDULER secret is missing!"
            exit 1
          fi
